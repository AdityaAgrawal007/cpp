#include <iostream>
#include <memory> // smart pointers are defined in the memory header 
using namespace std;

// wrapper class in cpp is a class that wraps around another class, it's used for managing that class/resource its like an interface to interact with it 

// smart pointer is a wrapper class around a raw pointer which automatically handles dealocation etc
// auto_ptr takes "ownership" of dynamically allocated objects and manages it's lifetime ensuring memory is freed when it goes out of scope

int main(){

}
