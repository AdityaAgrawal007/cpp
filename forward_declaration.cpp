#include <iostream> 
using namespace std;

// forward declaration is declaring something (class, function, variable) without defining it. enum and typedef cannot be forward declared cause they don't have diffrent seperate declaration and definition stages 
